https://nmap.org/man/it/index.html
https://it.wikipedia.org/wiki/Port_scanning

Networking
✔ Come comunicano i servizi nella rete?
I servizi per comunicare attraverso la rete hanno bisogno di:
IP Address + Porta + Protocollo

IP Address = identifica il PC nella rete (es: 142.184.78)
Porta = identifica il servizio (es: 443)
Protocollo = come comunicano (es: TCP)

✔ Cos'è una porta
Una porta è un numero che identifica un servizio. [0-65535]

22   SSH
80   HTTP
443  HTTPS
21   FTP
25   SMTP

✔ stati delle porte (open, closed, filtered)
- open: applicazione o servizio che accetta connessioni TCP, datagrammi UDP o associazioni SCTP. [Port is listening]
Se invii un SYN ad una porta aperta, dovresti ricevere un SYN/ACK
Client -> SYN
Server -> SYN/ACK
Client <- ACK

- closed: applicazione o servizio che NON accetta connessioni o ignora tutti i pacchetti in arrivo. [Port is not listening]
Se invii un SYN ad una porta chiusa, riceverai un RST

- filtered: applicazione o servizio che può accettare o no connessioni (non si può sapere), probabilmente dietro un firewall. [Port is (?)]
Se invii un SYN ad una porta filtrata, i pacchetti vengono buttati via e non riceverai nessuna risposta (neanche un RST)
Client -> SYN
Firewall - (blocca)
Client <- nessuna risposta

✔ IP address e Host
IP Address = Internet Protocol Address
IPv4 o IPv6

L'Indirizzo IP Pubblico è un numero identificativo univoco assegnato ad ogni dispostivo connesso ad Internet.
-- Ciò che ti identifica in rete, normalmente assegnato dal ISP (Wind, Tim, Vodafone, ...)
-- Modificabile con VPN
-- Se fai una richiesta a www.google.com con il telefono e 30 minuti dopo la fai con il PC, il server vede sempre lo stesso indirizzo IP 

Indirizzo IP Privato è un numero identificativo assegnato ad ogni dispostivo nella rete azeindale o domestica. 
-- Ciò che ti identifica nella rete interna, normalmente assegnato dal router o dal DHCP della rete locale
-- Modificabile manualmente ma può creare conflitti con altri dispostivi
-- Se l'amico A effettua una richiesta a www.google.com con il telefono con indirizzo IP privato XXX.YYY.ZZZ e l'amico B effettua la 
stessa richiesta con indirizzo privato IP XXX.YYY.KKK, entrambi riceveranno una risposta dal Server di Google poichè il NAT smisterà 
efficentemente le risposte.

Percorso:
richiesta con indirizzo privato -> router + NAT -> trasforma indirizzo privato in pubblico 
-> invio richiesta tramite internet -> Server riceve -> Server risponde -> invio richiesta tramite internet 
-> ricevo richiesta router + NAT -> trasformo indirizzo pubblico in privato -> invio risposta al dispostivo

✔ TCP vs UDP
Protocolli di trasporto (livello 4 "Trasport Layer" - OSI Model)
- TCP (Transmission Control Protocol):
Protcollo di trasporto più affidabile.
Prima dell'invio di un pacchetto, viene stabilita una connessione tra i due endpoint tramite Three-Way Handshake.
Quando il pacchetto viene ricevuto dal Server, quest'ultimo invia una conferma (ACK) terminando la connessione (FIN).
(es: TCP necessario per messaggistica, e-mail, altro..)

- UDP (User Datagram Protocol):
Protocollo di trasporto meno affidabile ma più veloce.
Non stabilisce una connessione ma invia i pacchetti direttamente, quelli persi saranno perduti per sempre. Il mittente non riceve nessuna
notifica di consegna o perdita del pacchetto.
(es: UDP necessaria per streaming video/audio, videogames online)

✔ Cos'è una Socket
Socket vuol dire "presa" in italiano ed è ciò che serve a due processi su endpoint differenti per comunicare.
Senza Socket i due processi non possono comunicare!
Ad ogni socket viene associato un IP Address e il numero di porta:
[IP_ADDRESS]:[PORT]

Sorgente -> Destinazione
192.168.1.10:54321 → 142.250.184.78:443 TCP

✔ Come funziona un Port Scanner
Network Tool che effettua uno scanner dell'Host Target e restitusce una lista delle porte con le relative informazioni.

✔ Cosa voglio che questo tool faccia?
1. EFFETTUARE SCANNER NELL'HOST TARGET
2. RESTITUIRE INFO RELATIVE ALLE PORTE



✔ PROGRAMMAZIONE TOOL:
Come fare?
Per eseguire la scansione, il programma dovrà inviare dei pacchetti TCP all'host target.
Quindi cercare di stabilire una connessione:
1. Invio di TCP packets con richiesta di tipo SYN all'host target (la socket permette di inviare pacchetti)
2. Aspettare pacchetto di ricezione dell'host target ed elaborare informazioni

Per farlo ho bisogno della Socket TCP:
Devo creare una socket e implementare le funzioni.
Elaborare le informazioni ricevute come mi pare.

Quindi:
1. Il programma crea una TCP Socket
2. Il SO invia una pacchetto SYN all'host target
3. Il target risponde
4. Elaborazione delle informazioni di risposta





Python
✔ socket module
✔ socket.connect_ex()
✔ socket.settimeout()
✔ for loop
✔ funzioni
✔ try/except